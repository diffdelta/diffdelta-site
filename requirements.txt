# Pinned versions for security and reproducibility
# Last audited: 2026-02-05
requests==2.32.5  # Fixes CVE-2024-35195 (Sticky SSL vulnerability)
jsonschema==4.26.0  # Required for Schema 1.0.0 validation logic
feedparser==6.0.12  # Closes potential XXE (XML External Entity) entry points
beautifulsoup4==4.12.3  # Stable and secure when paired with html.parser
# lxml removed: not used in code (using html.parser instead), reduces attack surface